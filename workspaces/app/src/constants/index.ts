export enum ROUTE {
  HOME = '/',
  ANALYSE = '/analyse',
  BROWSE = '/browse',
  COMPARE = '/compare',
  INFO = '/info',
}

export enum PROVIDER {
    STEAM = 'steam',
    LOCAL = 'upload',
    MODIO = 'modio',
}

export enum BROWSE_PARTS {
    SEARCH = 'search',
    AUTHOR = 'author',
    COLLECTION = 'collection',
    FILTER = 'filter',
    SORT = 'sort',
}
