export enum ROUTE {
  HOME = '/',
  ANALYSE = '/analyse',
  BROWSE = '/browse',
  COMPARE = '/compare',
  INFO = '/info',
}

export enum PROVIDER {
    STEAM = 'steam',
    LOCAL = 'upload',
    MODIO = 'modio',
}

export enum BROWSE_PARTS {
    CRITERION = 'criterion',
    SEARCH = 'search',
    AUTHOR = 'author',
    COLLECTION = 'collection',
}
